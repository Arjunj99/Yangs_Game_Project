using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GroundSensorConsolidated : MonoBehaviour
{

	public bool playerTouchingGround;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    	//  if(playerTouchingGround)
    	// 	Debug.Log("Player is touching ground");

    	// else if(!playerTouchingGround)
    	// 	Debug.Log("Player is not touching ground");
        
    }
}
